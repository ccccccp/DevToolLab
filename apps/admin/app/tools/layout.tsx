export const metadata = {
  title: "工具管理 | DevToolLab Admin",
  description: "DevToolLab 后台工具管理页面"
};

export default function ToolsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
