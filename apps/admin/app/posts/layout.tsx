export const metadata = {
  title: "文章管理 | DevToolLab Admin",
  description: "DevToolLab 后台文章管理页面"
};

export default function PostsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
