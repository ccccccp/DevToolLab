export const metadata = {
  title: "用户管理 | DevToolLab Admin",
  description: "DevToolLab 后台用户管理页面"
};

export default function UsersLayout({ children }: { children: React.ReactNode }) {
  return children;
}
