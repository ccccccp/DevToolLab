export const metadata = {
  title: "登录 | DevToolLab Admin",
  description: "DevToolLab 后台登录页面"
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
